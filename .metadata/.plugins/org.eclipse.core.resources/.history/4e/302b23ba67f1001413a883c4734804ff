public class Problem8 {

	public static void main(String[] args) {
		// Find the maximum of 5 integers
		
		int numA = 12;
		int numB = 37;
		int numC = 35;
		int numD = 3;
		int numE = 9;
		int maxPrime;
		
		if (Math.max(numA, numB) < numC){
			int x1 = Math.min(numA,numB);
			int x2 = Math.max(numA,numB);
			int max = numC;	
			if (Math.min(numD,numE) > x3){
				int x4 = Math.min(numD,numE);
				int x5 = Math.max(numD,numE);
				System.out.println(x1 + "<" + x2 + "<" + x3 + "<" + x4 + "<" + x5);
		} else {
			int x3 = Math.max(numA, numB);
			if (numC < Math.min(numA,numB)){
				int x1 = numC;
				int x2 = Math.min(numA,numB);
			}
			if (Math.min(numD,numE) > x3){
				int x4 = Math.min(numD,numE);
				int x5 = Math.max(numD,numE);
				System.out.println(x1 + "<" + x2 + "<" + x3 + "<" + x4 + "<" + x5)
		}
		

		} else {
			int x3 = Math.max(numA, numB);
			if (numC < Math.min(numA,numB)){
				int x1 = numC;
				int x2 = Math.min(numA,numB);
			}
		}
	}
}
